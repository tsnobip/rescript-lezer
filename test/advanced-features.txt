# Module with body

module M = {
  let x = 1
}

==>

Script(ModuleDeclaration(module,ConstructorName,ModuleBody(LetBinding(let,VariableDefinition,Number))))

# Nested modules

module Outer = {
  module Inner = {
    let value = 42
  }
}

==>

Script(ModuleDeclaration(module,ConstructorName,ModuleBody(
  ModuleDeclaration(module,ConstructorName,ModuleBody(
    LetBinding(let,VariableDefinition,Number))))))



